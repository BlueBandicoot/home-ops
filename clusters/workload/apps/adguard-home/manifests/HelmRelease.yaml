---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: adguard-home
  namespace: adguard-home
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      adguard:
        replicas: 1
        strategy: RollingUpdate
        containers:
          app:
            image:
              repository: adguard/adguardhome
              tag: v0.107.64
            env:
              TZ: Europe/Brussels
    service:
      app:
        controller: adguard
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: 192.168.129.200
          lbipam.cilium.io/sharing-key: "adguard-home"
        ports:
          http:
            primary: true
            port: 3000
          dns-tcp:
            port: 53
            protocol: TCP
          dns-udp:
            port: 53
            protocol: UDP
    route:
      app:
        hostnames: ["adguard.kharnox.com"]
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https-adguard
    persistence:
      config:
        enabled: true
        existingClaim: adguard-conf
        globalMounts:
          - path: /opt/adguardhome/conf
      work:
        enabled: true
        existingClaim: adguard-work
        size: 5Gi
        globalMounts:
          - path: /opt/adguardhome/work
